; PHP upload limits for admin branding uploads (logo / login background).
; Many hosts default to 2M, which causes empty multipart bodies and 422 "required" errors.
; Reload PHP-FPM / redeploy after changing.
upload_max_filesize = 32M
post_max_size = 32M
